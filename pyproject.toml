[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "enterprise-sales-revenue-intelligence"
version = "0.1.0"
description = "Synthetic-data sales and revenue intelligence portfolio project"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
