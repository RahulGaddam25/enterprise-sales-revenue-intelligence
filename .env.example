# This project runs entirely on synthetic local data by default.
# Set DATABASE_URL only when loading the generated CSV files into PostgreSQL.
DATABASE_URL=postgresql://user:password@localhost:5432/sales_intelligence
